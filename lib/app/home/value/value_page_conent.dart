import 'package:flutter/material.dart';

class ValuePageContent extends StatelessWidget {
  const ValuePageContent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dwa'),
    );
  }
}

